/**
 * WebViewScreen Styles - Style definitions for WebViewScreen component
 */

import { StyleSheet } from 'react-native';

export const webViewScreenStyles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#f5f5f5',
  },
  webview: {
    flex: 1,
  },
});
